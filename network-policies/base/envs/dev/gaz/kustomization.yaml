apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: gaz-dev

resources:
  - namespace.yaml
  - baseline.yaml

labels:
  - pairs:
      app.kubernetes.io/managed-by: argocd
      app.kubernetes.io/instance: network-policies-base
      app.kubernetes.io/component: baseline
      env: dev
      policy-tier: baseline
