apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: allow-bot-egress
  namespace: discord-bot
spec:
  podSelector:
    matchLabels:
      app: discord-kube-bot
  policyTypes:
  - Egress
  egress:
  - ports:
    - port: 443
      protocol: TCP
    - port: 6443
      protocol: TCP
