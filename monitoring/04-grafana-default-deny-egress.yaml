apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: grafana-default-deny-egress
  namespace: monitoring
spec:
  podSelector:
    matchLabels:
      app: grafana
  policyTypes:
  - Egress
