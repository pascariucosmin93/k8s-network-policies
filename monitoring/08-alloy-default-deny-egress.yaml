apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: alloy-default-deny-egress
  namespace: monitoring
spec:
  podSelector:
    matchLabels:
      app.kubernetes.io/instance: alloy
      app.kubernetes.io/name: alloy
  policyTypes:
  - Egress
